# MOEX Analytics MCP - Cloudflare Worker

Remote MCP server backed by the official Moscow Exchange ISS API.

Endpoints after deployment:
- `/mcp` - MCP Streamable HTTP
- `/health` - health check

Cloudflare build/deploy command:
`npm run deploy`
